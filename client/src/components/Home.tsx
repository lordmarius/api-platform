'use client'

import { Card, List, ListItem } from "@tremor/react"

const Home = () => {

    // const { data, isLoading } = useQuery({
    //     queryKey: ['greetings'],
    //     queryFn: () =>
    //         fetch("/greetings").then((res) => {
    //             return res.json()
    //         })
    // })

    // if (isLoading) return <Card className="w-6/12">Cargando...</Card>

    // const greetings = data['hydra:member']

    return "Hola"
}

export default Home